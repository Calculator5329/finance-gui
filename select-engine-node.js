// Simple script to select the engine node via browser console
// Run this in the browser dev tools console:
//
// window.__rootStore?.flowStore?.selectNode('node-engine');
//
// Or open this URL in the address bar (won't work due to JavaScript URI restrictions in modern browsers):
// javascript:window.__rootStore?.flowStore?.selectNode('node-engine');

console.log('To select the engine node, run:');
console.log('window.__rootStore?.flowStore?.selectNode("node-engine");');
